[package]
name = "qbo-desktop-commands"
version = "0.1.0"
edition.workspace = true
description = "The read-only query API as one Tauri-command-shaped function, so the Mac session's #[tauri::command] body is one call into this crate."

[dependencies]
qbo-local = { path = "../../qbo-local" }
serde_json = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
chrono = { workspace = true }
serde_json = { workspace = true }
